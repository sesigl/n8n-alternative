{
  "name": "n8n-alternative",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Modern workflow automation platform",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm --filter './apps/*' --filter './services/*' build",
    "test": "pnpm vitest run",
    "test:coverage": "pnpm vitest run --coverage",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "check": "pnpm check:pinned-versions && pnpm lint && pnpm check:types && pnpm test",
    "check:types": "pnpm -r check:types",
    "check:pinned-versions": "bash scripts/check-pinned-versions.sh",
    "clean": "pnpm -r exec rm -rf dist && rm -rf node_modules",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.2",
    "@changesets/cli": "2.29.7",
    "@vitest/coverage-v8": "4.0.6",
    "typescript": "5.9.3",
    "vite-tsconfig-paths": "5.1.4",
    "vitest": "4.0.6"
  },
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": "24.11.0",
    "pnpm": "10.20.0"
  }
}
